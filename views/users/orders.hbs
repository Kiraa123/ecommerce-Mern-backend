<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Orders</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/cart.css' />
</head>

<body>
            {{> userheader}}

    <div class="container mt-4">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title"><b>Your Orders</b></h4>

                <!-- Check if there are orders -->
                {{#if data}}
                    {{#each data}}
                        <div class="order-item mb-4">
                            <div class="order-header bg-secondary text-white">
                                <div class="order-id">ORDER ID: {{orderID}}</div>
                                <div class="order-status">Order Status: {{status}}</div>
                            </div>

                            {{#each items}}
                                <div class="order-details row mb-2 mt-1">
                                    <div class="col"><img class="img-fluid" style="height: 50px;width:40px;"
                                            src="../uploads/{{this.product.image}}"></div>
                                    <div class="col">{{this.product.productname}}</div>
                                    <div class="col text-muted">Qty:{{this.quantity}}</div>
                                    <div class="col text-muted">Size: {{this.size}}</div>
                                    <div class="col text-muted">Unit Price:{{this.product.price}}</div>
                                </div>
                            {{/each}}

                            <div class="order-summary row mb-2 mt-1 ">
                                {{!-- <div class="col-8 text-muted">Date: {{formatDate orderdate}}</div> --}}
                                <div class="col-4">Total Price: {{totalamount}}</div>
                            </div>
                        </div>
                    {{/each}}
                {{else}}
                    <h3>No Orders.</h3>
                    <a href="/users/allproducts" class="btn btn-success">Add Product</a>
                {{/if}}

                <div class="back-to-shop mt-3">
                    <a href="/">&leftarrow;</a>
                    <span class="text-muted">Back to shop</span>
                </div>
            </div>
        </div>

       
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>
</body>

</html>
